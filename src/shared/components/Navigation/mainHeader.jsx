import React from 'react';

const MainHeader = (props) => {
    return (
        <header className="bg-primary-red h-header">
            <div>header</div>
        </header>
    );
};

export default MainHeader;
